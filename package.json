{
  "name": "approved-by-action",
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js -o dist --source-map"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.0"
  }
}
